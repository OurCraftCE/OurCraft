#pragma once

// Core's precompiled header simply re-uses the Client's PCH.
// The server classes were originally compiled with the Client's stdafx.h
// and depend on everything it pulls in.
#include "../Client/stdafx.h"
