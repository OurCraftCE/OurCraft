#include "stdafx.h"
#include "DiscordXUIDMapper.h"

// DiscordXUIDMapper is a header-only utility class with all static inline methods.
// This .cpp exists to ensure the header compiles cleanly and to provide a compilation
// unit if non-inline helpers are needed in the future.
